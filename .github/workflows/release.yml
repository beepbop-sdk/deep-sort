name: Release

on:
  workflow_dispatch:
    inputs:
      bump-level:
        description: "Version bump level"
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch
      force-publish:
        description: "Skip change detection and always publish"
        type: boolean
        default: false
  push:
    branches:
      - main
      - dev

permissions:
  contents: write
  packages: write
  pull-requests: write
  statuses: write

jobs:
  release:
    uses: beepbop-labs/github-actions/.github/workflows/npm-release.yml@main
    with:
      bump-level: ${{ inputs.bump-level || 'patch' }}
      force-publish: ${{ inputs.force-publish || false }}
    secrets:
      npm-token: ${{ secrets.NPM_TOKEN }}
